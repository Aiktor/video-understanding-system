# 🎥 Video Understanding System

**Комплексная система анализа видео с AI-агентами на базе LangChain и Pydantic**

## 📋 Описание проекта

Video Understanding System — это мощная платформа для анализа видеоконтента с использованием искусственного интеллекта. Система предоставляет несколько режимов работы для различных сценариев использования:

### 🎯 Основные возможности

1. **Анализ видеофайлов** - Проверка соответствия действий в записанном видео техническим инструкциям
2. **Real-time мониторинг** - Отслеживание выполнения инструкций в режиме реального времени через веб-камеру
3. **Счетчик упражнений** - Автоматический подсчет повторений упражнений (приседания, отжимания и др.)
4. **Запись для экономии** - Запись видео с последующим анализом дешевой моделью (экономия до 90% на API)

### 💡 Применение

- **Производство** - Контроль соблюдения технологических процессов
- **Обучение** - Проверка правильности выполнения учебных заданий
- **Фитнес** - Подсчет повторений упражнений и контроль техники
- **Контроль качества** - Мониторинг выполнения стандартов и процедур

### 🏆 Преимущества

- ✅ Гибкая архитектура с модульной структурой
- ✅ Настраиваемая производительность (FPS, разрешение, интервал анализа)
- ✅ Выбор между скоростью (real-time) и экономией (запись + анализ)
- ✅ Автоматическая оптимизация параметров
- ✅ Подробная документация и готовые примеры
- ✅ Поддержка различных AI моделей (дорогие/быстрые или дешевые/медленные)

## 📁 Структура проекта

```
VideoUnderstand/
├── models.py              # Pydantic модели данных
├── video_processor.py     # Обработка видеофайлов
├── webcam_processor.py    # Обработка веб-камеры ✨
├── webcam_recorder.py     # Запись видео с камеры 🎬
├── ai_agent.py           # AI-агент для анализа (LangChain)
├── analyzer.py           # Логика анализа видеофайлов
├── live_analyzer.py      # Анализ в реальном времени ✨
├── squats_counter.py     # Счетчик приседаний 🏋️
├── utils.py              # Вспомогательные функции
├── analyze_video.py      # CLI для анализа видеофайлов
├── analyze_webcam.py     # CLI для анализа веб-камеры ✨
├── record_webcam.py      # CLI для записи видео 🎬
├── count_squats.py       # CLI для подсчета приседаний 🏋️
├── requirements.txt      # Зависимости
├── instruction_example.json  # Пример инструкции
├── WEBCAM_GUIDE.md       # Руководство по веб-камере
├── RECORD_GUIDE.md       # Запись видео для анализа 🎬
├── SQUATS_GUIDE.md       # Руководство по счетчику приседаний
├── PERFORMANCE_GUIDE.md  # Настройка производительности ⚙️
├── SETTINGS_QUICK.md     # Быстрая настройка ⚡
├── FPS_VS_INTERVAL.md    # Разница FPS и Interval 💡
└── .env                  # Конфигурация (API ключи)
```

## 🚀 Быстрый старт

### Установка

```bash
pip install -r requirements.txt
```

### Конфигурация

Создайте файл `.env`:

```env
PROXY_API_KEY=your-api-key-here
PROXY_BASE_URL=https://api.proxyapi.ru/openai/v1
PROXY_PROVIDER=openai
```

### Использование

**Анализ видеофайла:**

```bash
python analyze_video.py video.mp4 instruction_example.json
```

**Анализ веб-камеры в реальном времени:** ✨

```bash
python analyze_webcam.py instruction_example.json
```

**Счетчик приседаний:** 🏋️

```bash
# Базовый запуск (авто-настройки)
python count_squats.py

# Быстрые приседания
python count_squats.py --interval 0.3  # fps будет auto (7)

# Ручные настройки
python count_squats.py --interval 0.5 --fps 10 --width 320 --height 240 --quality 70
```

**Запись видео для анализа:** 🎬

```bash
# Записать видео
python record_webcam.py workout.mp4 --duration 300

# Проанализировать с дешевой моделью
python analyze_video.py workout.mp4 instruction.json --interval 3.0
```

С дополнительными параметрами:

```bash
# Видеофайл
python analyze_video.py video.mp4 instruction.json --interval 3.0 --output result.json

# Веб-камера
python analyze_webcam.py instruction.json --camera 0 --interval 5.0 --no-display

# Счетчик приседаний - настройки производительности
python count_squats.py --interval 0.3  # Быстрые приседания
python count_squats.py --interval 1.0 --fps 10 --quality 60  # Экономия ресурсов
```
файлами:
- Извлечение кадров с заданным интервалом
- Кодирование в base64
- Context manager для автоматического освобождения ресурсов

### webcam_processor.py ✨ NEW
Класс `WebcamProcessor` для работы с веб-камерой:
- Захват видеопотока в реальном времени
- Отображение видео с наложенным текстом
- Управление камерой

### ai_agent.py
Класс `VideoAnalysisAgent` для AI-анализа:
- `analyze_video_sequence()` - анализ последовательности кадров
- `analyze_frame()` - анализ отдельного кадра
- `match_with_instruction()` - сопоставление с инструкцией
- `generate_video_summary()` - генерация общего описания видео
- Интеграция с LangChain и ChatOpenAI

### analyzer.py
Функции анализа видеофайлов:
- `analyze_video()` - основная функция анализа видео
- `generate_summary()` - генерация текстового резюме

### live_analyzer.py ✨ NEW
Класс `LiveAnalyzer` для анализа в реальном времени:
- Отслеживание текущего шага инструкции
- Проверка соблюдения порядка действий
- Генерация предупреждений при отклонениях
- Ведение истории действий

### analyze_video.py
CLI приложение для анализа видеофайлов

### analyze_webcam.py ✨ NEW
CLI приложение для анализа веб-камеры в реальном временииза:
- `analyze_video_sequence()` - анализ последовательности кадров
- `match_with_instruction()` - сопоставление с инструкцией
- `generate_video_summary()` - генерация общего описания видео
- Интеграция с LangChain и ChatOpenAI

### analyzer.py
Функции анализа:
- `analyze_video()` - основная функция анализа видео
- `generate_summary()` - генерация текстового резюме

### analyze_video.py
CLI приложение для анализа видеофайлов

## 💡 Пример программного использования

```python
import os
from dotenv import load_dotenv
from models import Instruction, InstructionStep
from analyzer import analyze_video

load_dotenv()

# Создаем инструкцию
instruction = Instruction(
    title="Производство гипсовых панелей",
    steps=[
        InstructionStep(
            step_number=1,
            description="Подготовка формы",
            expected_duration_seconds=120,
            critical=True
        ),
        InstructionStep(
            step_number=2,
            description="Приготовление раствора",
            expected_duration_seconds=180,
            critical=True
        )
    ]Возможности

### Анализ видеофайлов
- ✅ Извлечение и анализ кадров
- ✅ Сопоставление с инструкцией
- ✅ Генерация описания видео
- ✅ Детальное резюме с временными метками
- ✅ Сохранение результатов в JSON

### Анализ веб-камеры ✨
- ✅ Захват видео в реальном времени
- ✅ Отслеживание текущего шага инструкции
- ✅ Проверка правильности последовательности
- ✅ Предупреждения при нарушении порядка
- ✅ Визуализация прогресса
- ✅ Автоматическая остановка при завершении

### Счетчик упражнений 🏋️
- ✅ Автоматический подсчет приседаний
- ✅ Определение состояния тела (стоит/приседает)
- ✅ Отображение счетчика в реальном времени
- ✅ Настраиваемый интервал анализа
- ✅ Итоговая статистика

### Запись видео 🎬
- ✅ Запись с веб-камеры в файл
- ✅ Настраиваемое разрешение и FPS
- ✅ Обратный отсчет перед записью
- ✅ Управление записью (пауза/остановка)
- ✅ Экономия 75% на API (дешевая модель для анализа)

## 🎯 Примеры использования

### 1. Анализ готового видео

```bash
python analyze_video.py video.mp4 instruction_example.json --output result.json
```

**Результат:**
- Описание всего видео от AI
- Список всех обнаруженных действий
- Сопоставление с каждым шагом инструкции
- Временные метки для каждого действия
- JSON файл с полными результатами

### 2. Мониторинг процесса в реальном времени

```bash
python analyze_webcam.py instruction_example.json --interval 5.0
```

**Возможности:**
- Видео с камеры с наложением статуса
- Текущий прогресс выполнения
- Ожидаемый следующий шаг
- Предупреждения о нарушениях
- Итоговое резюме после завершения

### 3. Подсчет приседаний 🏋️

```bash
python count_squats.py
```

**Возможности:**
- Автоматический подсчет каждого приседания
- Визуализация на видео и в консоли
- Настраиваемый интервал анализа
- Итоговая статистика

### 4. Консольный режим (без GUI)

```bash
python analyze_webcam.py instruction.json --no-display
```

Подходит для:
- Серверов без GUI
- Автоматизации
- Удаленного мониторингаKEY"),
    frame_interval=2.0
)

# Работаем с результатами
print(result.video_summary)
print(f"Выполнено шагов: {len([m for m in result.action_matches if m.matched])}/{len(instruction.steps)}")
print(f"Пропущенные шаги: {result.missing_steps}")
```

## 🔄 Roadmap

- [x] Анализ видеофайлов
- [x] Сопоставление с инструкцией
- [x] Генерация описания видео
- [x] Анализ веб-камеры в реальном времени
- [x] Мониторинг соблюдения порядка действий
- [x] Счетчик упражнений (приседания)
- [x] Настройка производительности
- [x] Запись видео для экономии на API
- [ ] Другие типы упражнений (отжимания, подтягивания)
- [ ] Telegram бот для уведомлений

## ⚙️ Настройка производительности

Если счетчик приседаний не распознает движения или работает медленно:

### Быстрое решение
```bash
# Уменьшите интервал для лучшего распознавания
python count_squats.py --interval 0.5

# Для быстрых приседаний
python count_squats.py --interval 0.3
```

### Оптимизация ресурсов
```bash
# Низкое разрешение = быстрее обработка
python count_squats.py --width 320 --height 240 --quality 60

# Для слабых компьютеров
python count_squats.py --fps 10 --width 320 --height 240 --quality 60 --no-display
```

### Параметры производительности

| Параметр | Описание | Рекомендуемое | Влияние |
|----------|----------|---------------|---------|
| `--interval` | Частота анализа (сек) | 0.3-0.5 | Скорость распознавания ⚡ |
| `--fps` | Кадров в секунду | 0 (auto) | Плавность видео 🎥 |
| `--width` | Ширина кадра | 320 | Размер данных 📦 |
| `--height` | Высота кадра | 240 | Размер данных 📦 |
| `--quality` | Качество JPEG (1-100) | 60-70 | Сжатие данных 🗜️ |

**💡 Важно:** `--fps` влияет только на плавность видео, НЕ на частоту AI анализа!  
Авто-режим (`--fps 0`) автоматически рассчитает оптимальный FPS на основе interval.

📖 **Подробное руководство:** [PERFORMANCE_GUIDE.md](PERFORMANCE_GUIDE.md)  
⚡ **Быстрая настройка:** [SETTINGS_QUICK.md](SETTINGS_QUICK.md)  
🎯 **FPS vs Interval:** [FPS_VS_INTERVAL.md](FPS_VS_INTERVAL.md)

## �️ Технологии

- **LangChain** - фреймворк для AI-агентов
- **Pydantic** - валидация и структурирование данных
- **OpenCV (cv2)** - обработка видео и захват с камеры
- **OpenAI GPT-4o/GPT-4o-mini/GPT-5-nano** - мультимодальные модели для анализа
- **Python 3.13+** - основной язык разработки
## 🎓 Примеры использования

### 1️⃣ Контроль производственного процесса

```bash
# Real-time мониторинг
python analyze_webcam.py production_steps.json

# Или запись смены и анализ потом
python record_webcam.py shift.mp4 --duration 28800
python analyze_video.py shift.mp4 production_steps.json --interval 10.0
```

### 2️⃣ Подсчет приседаний на тренировке

```bash
# Real-time подсчет
python count_squats.py --interval 0.3

# Или запись и анализ
python record_webcam.py workout.mp4 --duration 600
python analyze_video.py workout.mp4 squats_instruction.json
```

### 3️⃣ Обучение персонала

```bash
# Запись демонстрации
python record_webcam.py training_demo.mp4 --duration 300

# Проверка выполнения учеником
python analyze_video.py student_video.mp4 training_steps.json --output result.json
```

## 💰 Экономия на API

| Подход | Запросов/10мин | Модель | Стоимость |
|--------|----------------|--------|-----------|
| Real-time (interval=0.5) | ~1200 | Дорогая | $$$$ |
| Запись + Анализ (interval=5.0) | ~120 | Дешевая | $ |

**Экономия: до 90%!** 💸
## 📦 Структура файлов проекта

### 🟢 Основные модули (необходимы)

**Модели и утилиты:**
- `models.py` - Pydantic модели данных
- `utils.py` - Вспомогательные функции (логирование)
- `requirements.txt` - Зависимости проекта
- `.env` - Конфигурация API (создается пользователем)

**Обработчики видео:**
- `video_processor.py` - Обработка видеофайлов
- `webcam_processor.py` - Захват видео с камеры
- `webcam_recorder.py` - Запись видео в файл

**AI и анализ:**
- `ai_agent.py` - AI-агент на LangChain
- `analyzer.py` - Анализ видеофайлов
- `live_analyzer.py` - Анализ в реальном времени
- `squats_counter.py` - Счетчик упражнений

**CLI приложения:**
- `analyze_video.py` - Анализ записанного видео
- `analyze_webcam.py` - Real-time анализ с камеры
- `record_webcam.py` - Запись видео с камеры
- `count_squats.py` - Подсчет приседаний

**Примеры:**
- `instruction_example.json` - Пример инструкции (производство)
- `instruction_squats.json` - Пример для приседаний

### 📘 Документация (рекомендуется)

- `README.md` - Главная документация
- `WEBCAM_GUIDE.md` - Руководство по веб-камере
- `RECORD_GUIDE.md` - Запись видео для анализа
- `SQUATS_GUIDE.md` - Счетчик приседаний
- `PERFORMANCE_GUIDE.md` - Настройка производительности
- `SETTINGS_QUICK.md` - Быстрые рецепты настройки
- `FPS_VS_INTERVAL.md` - Объяснение параметров
- `WORKFLOW_RECORD.md` - Workflow для экономии
- `TROUBLESHOOTING.md` - Решение проблем
- `SQUATS_QUICKSTART.md` - Быстрый старт для приседаний

### 🔴 Можно удалить

**Служебные файлы разработки:**
- `CHANGELOG_PERFORMANCE.md` - История изменений (для разработчиков)
- `notebook.ipynb` - Jupyter notebook для экспериментов
- `__pycache__/` - Кэш Python (автоматически создается)

**Тестовые файлы:**
- `video.mp4` - Тестовое видео
- `video1.mp4` - Тестовое видео
- `my_video.mp4` - Записанное видео
- `gpt_cost.docx` - Расчеты стоимости
- `~$t_cost.docx`, `~WRL0029.tmp` - Временные файлы Word

**Итого для удаления:**
```bash
# Временные и тестовые файлы
rm CHANGELOG_PERFORMANCE.md
rm notebook.ipynb
rm video.mp4 video1.mp4 my_video.mp4
rm gpt_cost.docx
rm -r __pycache__/
```

После удаления остается **~25 файлов** (код + документация), занимающих **~200 КБ** без тестовых видео.

## �📝 Формат инструкции

```json
{
  "title": "Название процесса",
  "steps": [
    {
      "step_number": 1,
      "description": "Описание шага",
      "expected_duration_seconds": 120,
      "critical": true
    }
  ]
}
```

## 🛠️ Технологии

- **LangChain** - фреймворк для AI-агентов
- **Pydantic** - валидация и структурирование данных
- **OpenCV** - обработка видео
- **OpenAI GPT-4o/GPT-4o-mini** - мультимодальная модель для анализа

## 📄 Лицензия

MIT
